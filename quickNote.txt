1. For AggieFeed, after clone
	git clone https://Charlie-Ng@bitbucket.org/ucdavis/edustream-www.git
	git fetch
	git branch
	git branch -r
	sudo npm install
	cd app (where bower.json located)
	bower install

	grunt start
	grunt (in a different window)